meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/identity/resources
  body: json
  auth: bearer
}

body:json {
  {
    "key": "users",
    "description": "User management resource"
  }
}

auth:bearer {
  token: {{access_token}}
}
