meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/identity/permissions/1
  body: json
  auth: bearer
}

body:json {
  {
    "description": "Read only access",
    "scopes": 2
  }
}

auth:bearer {
  token: {{access_token}}
}
