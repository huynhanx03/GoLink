meta {
  name: Change Password
  type: http
  seq: 4
}

post {
  url: {{base_url}}/identity/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "current_password": "123456789",
    "new_password": "123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
