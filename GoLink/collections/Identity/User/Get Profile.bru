meta {
  name: Get Profile
  type: http
  seq: 3
}

get {
  url: {{base_url}}/identity/users/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
