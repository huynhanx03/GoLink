meta {
  name: Update
  type: http
  seq: 3
}

put {
  url: {{base_url}}/identity/tenants/1
  body: json
  auth: bearer
}

body:json {
  {
    "name": "Acme Corporation"
  }
}

auth:bearer {
  token: {{access_token}}
}
