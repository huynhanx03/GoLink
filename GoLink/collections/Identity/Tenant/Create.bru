meta {
  name: Create
  type: http
  seq: 2
}

post {
  url: {{base_url}}/identity/tenants
  body: json
  auth: bearer
}

body:json {
  {
    "name": "Acme Corp"
  }
}

auth:bearer {
  token: {{access_token}}
}
