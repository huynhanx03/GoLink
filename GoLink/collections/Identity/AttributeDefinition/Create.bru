meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/identity/attribute-definitions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "key": "department1",
    "data_type": "string",
    "description": "User department"
  }
}
